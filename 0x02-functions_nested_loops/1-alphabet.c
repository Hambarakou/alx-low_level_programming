#include "main.h"
#include <stdio.h>
/**
* main - Entry point
* function - print_alphabet
* Return: Always 0 (Success)
*/
int main(void)
{
void print_alphabet(void)
{
char ch;
for (ch = 'a' ; ch <= 'z' ; ch++)
{
putchar(ch);
}
return;
}
return (0);
}

#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
char n;
char i;
for (n = '0' ; n <= '9' ; n++)
{
putchar (n);
}
for (i = 'a' ; i <= 'f' ; i++)
{
putchar (i);
}
return (0);
}
